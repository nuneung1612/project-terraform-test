# =========================================================
# 0_backend_bootstrap/providers.tf
# =========================================================
provider "aws" {
  region = var.aws_region
}
